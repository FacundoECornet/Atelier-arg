---
phase: 01-foundation-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/Admin/firestoreApi.js
  - src/Componentes/Equipo.jsx
  - src/Componentes/Social.jsx
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03
  - CLN-04

must_haves:
  truths:
    - "npm run build completes without @dnd-kit/* packages in bundle"
    - "bulkUpdateOrden function no longer exists in firestoreApi.js"
    - "Zero <style jsx> blocks in any JSX source file"
    - "styled-components is not imported by any JSX file"
    - "npm run build completes without styled-components in bundle"
  artifacts:
    - path: "package.json"
      provides: "Clean dependency list without @dnd-kit/* or styled-components"
      grep_check: "grep -v '^#' package.json | grep -c '@dnd-kit' || echo '0'"
    - path: "src/Admin/firestoreApi.js"
      provides: "Firestore CRUD helpers without orphaned bulkUpdateOrden"
      grep_check: "grep -v '^#' src/Admin/firestoreApi.js | grep -c 'bulkUpdateOrden' || echo '0'"
    - path: "src/Componentes/Equipo.jsx"
      provides: "Team section without <style jsx> block"
      grep_check: "grep -v '^#' src/Componentes/Equipo.jsx | grep -c '<style jsx' || echo '0'"
    - path: "src/Componentes/Social.jsx"
      provides: "Social icons with Tailwind hover effects, no styled-components"
      grep_check: "grep -v '^#' src/Componentes/Social.jsx | grep -c 'styled-components' || echo '0'"
  key_links:
    - from: "src/Componentes/Social.jsx"
      to: "package.json"
      via: "After Tailwind conversion, no file imports styled-components — dependency removed"
    - from: "src/Componentes/Equipo.jsx"
      to: "tailwind.config.js"
      via: "line-clamp-3 is built-in Tailwind 3.3+ utility — custom CSS removed"
---

<objective>
Remove all orphaned dependencies and stale code. Uninstall @dnd-kit/* packages, remove bulkUpdateOrden function, eliminate `<style jsx>` blocks, and convert styled-components to Tailwind utilities. Zero user-facing changes — all functionality preserved.

Purpose: Clean dependency tree prevents build bloat and confusion. Standardizing on Tailwind eliminates styling fragmentation.
Output: Clean package.json (no orphaned deps), zero `<style jsx>` blocks, zero styled-components imports, all styles in Tailwind.
</objective>

<execution_context>
@/home/facuc/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/facuc/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md

<interfaces>
Key patterns from existing codebase:

From src/Componentes/Social.jsx (styled-components):
- Currently wraps `<ul>` in `<StyledWrapper>` using `styled.div` template literal
- Tooltip hover uses `.filled` div with height 0→100% on hover, colored by `data-social` attribute
- Three social platforms: whatsapp (#128c7e), facebook (#3b5998), instagram (gradient)
- Icons use inline SVGs with `fill="currentColor"` — Tailwind `text-*` classes will control color

From src/Componentes/Equipo.jsx (lines 81-88, the `<style jsx>` block):
```
<style jsx>{`
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
`}</style>
```
- This styles `.line-clamp-3` class used on member description `<p>` (line 74)
- Tailwind 3.4 ships `line-clamp-3` as a core built-in utility — removing this CSS block is safe

From src/Admin/firestoreApi.js (lines 35-41):
```
export async function bulkUpdateOrden(collectionName, updates) {
  const batch = writeBatch(db);
  updates.forEach(({ id, orden }) => { batch.update(doc(db, collectionName, id), { orden }); });
  await batch.commit();
}
```
- Uses `writeBatch` from firebase/firestore import — ONLY used by this function
- After removing bulkUpdateOrden, also remove `writeBatch` from the import on line 12

From package.json:
- Dependencies to remove: `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities`, `styled-components`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uninstall orphaned dependencies</name>
  <files>package.json, package-lock.json</files>
  <action>
Implement D-04 (per CONTEXT.md) plus styled-components cleanup:

Run:
```bash
npm uninstall @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities styled-components
```

This removes four orphaned dependencies:
- **@dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities:** Removed drag-and-drop feature; no file imports or references these. Verified by grep: zero imports in `src/`.
- **styled-components:** After Task 2 converts Social.jsx to Tailwind, zero files will import `styled-components` (only `Social.jsx` line 2 `import styled from "styled-components"` uses it). Removed proactively since D-07 makes it orphaned.

Also verify `@types/react` and `@types/react-dom` (devDependencies) are safe to keep — they're TypeScript type stubs used by editor tooling even in JSX projects. Do NOT remove them.

After npm uninstall, verify:
- `grep -c '@dnd-kit' package.json` returns 0
- `grep -c 'styled-components' package.json` returns 0
</action>
<verify>
<automated>
1. `grep -c '@dnd-kit' package.json | grep -v '^#' ` returns 0 (or `grep` exits 1)
2. `grep -c 'styled-components' package.json | grep -v '^#' ` returns 0
3. `ls node_modules/@dnd-kit 2>/dev/null` returns nothing (exit code 2)
4. `ls node_modules/styled-components 2>/dev/null` returns nothing (exit code 2)
5. `npm ls @dnd-kit/core 2>&1 | grep -c '(empty)'` returns 1 (confirmed removed)
</automated>
</verify>
<done>
@dnd-kit/* and styled-components removed from package.json, package-lock.json, and node_modules. No files import them.
</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned code and convert styles to Tailwind</name>
  <files>src/Admin/firestoreApi.js, src/Componentes/Equipo.jsx, src/Componentes/Social.jsx</files>
  <action>
Implement D-05, D-06, D-07 (per CONTEXT.md). Apply all three changes — each targets a separate file, no conflicts.

**D-05 — Remove bulkUpdateOrden (src/Admin/firestoreApi.js):**
- Delete lines 35-41: the entire `bulkUpdateOrden` function including its JSDoc comment if present.
- Delete `writeBatch` from the import on line 12: change `writeBatch,` to nothing (remove it from the destructured import from `firebase/firestore`). `writeBatch` is ONLY used by `bulkUpdateOrden` — removing it avoids unused import warnings.
- The import line (currently line 12) should become: `} from 'firebase/firestore';` (remove `writeBatch,`).

**D-06 — Remove `<style jsx>` block (src/Componentes/Equipo.jsx, NOT Nosotros.jsx):**
CONTEXT.md D-06 says Nosotros.jsx, but the actual `<style jsx>` block is in Equipo.jsx lines 81-88. Nosotros.jsx has no `<style jsx>` — verified by full file read and grep.
- Delete lines 81-88: the entire `<style jsx>{`...`}</style>` block.
- The `line-clamp-3` class (used on line 74) is a Tailwind 3.4 built-in utility. Tailwind config at v3.4.17 includes `line-clamp` core plugin. No replacement CSS needed.
- Verify: `grep -r '<style jsx' src/` returns zero matches.

**D-07 — Convert styled-components to Tailwind (src/Componentes/Social.jsx):**
- Remove line 2: `import styled from "styled-components";`
- Remove lines 81-185: the entire `StyledWrapper = styled.div` block (the template literal from line 81 to the closing backtick on line 185).
- Remove the `<StyledWrapper>` opening tag on line 6 and closing `</StyledWrapper>` on line 77.
- The component now returns the `<ul>` directly (wrap in a fragment if needed — but the ul with className is fine standalone).

Tailwind replacements for the CSS effects (apply to existing elements, not create new wrappers):
1. **ul list-style:** Add `list-none` class to `<ul className="example-2">` — the existing class `example-2` stays (no behavior change), add `list-none` for the `list-style: none` effect.
2. **ul flex layout:** `example-2` already has `display: flex; justify-content: center; align-items: center` in CSS. Add Tailwind: `flex justify-center items-center` to the ul. The `example-2` className serves no purpose after conversion but keep it — it may be used elsewhere. The Tailwind classes provide the actual styling.
3. **icon-content (each li):** `margin: 0 10px; position: relative` → add `mx-2.5 relative` to each `<li className="icon-content">` element.
4. **tooltip div (each li):** The tooltip `<div className="tooltip">` inside each li needs:
   - `absolute -top-[30px] left-1/2 -translate-x-1/2 text-white px-2.5 py-1.5 rounded-md opacity-0 invisible text-sm transition-all duration-300 pointer-events-none` as base classes
   - `group-hover:opacity-100 group-hover:visible group-hover:-top-[50px]` for hover effect
   - Background color via Tailwind arbitrary values or inline style — use Tailwind arbitrary values: for WhatsApp tooltip add `bg-[#128c7e]`, for Facebook add `bg-[#3b5998]`, for Instagram add `bg-gradient-to-r from-[#405de6] via-[#c135b4] to-[#fd1f1f]`
   - **Important:** Add `group` class to the parent `<li className="icon-content">` so `group-hover:` selectors work on tooltip children.
5. **anchor (a) inside each li:** The `<a>` tags need:
   - `relative overflow-hidden flex justify-center items-center w-[50px] h-[50px] rounded-full text-[#4d4d4d] bg-white transition-all duration-300` as base
   - `hover:shadow-lg hover:text-white` for hover effect
6. **svg inside anchor:** `relative z-10 w-[30px] h-[30px]` — use arbitrary values `w-[30px] h-[30px]`
7. **filled div (each li):** The `<div className="filled">` inside each anchor needs:
   - `absolute bottom-0 left-0 w-full h-0 transition-all duration-300` as base
   - `group-hover:h-full` for hover fill effect (note: `group` must be on the `a` tag, not the `li` — add `group` to each `<a>` tag)
   - Background color via arbitrary values matching tooltip: WhatsApp `bg-[#128c7e]`, Facebook `bg-[#3b5998]`, Instagram `bg-gradient-to-r from-[#405de6] via-[#c135b4] to-[#fd1f1f]`

**CSS to Tailwind mapping summary:**

| CSS Rule | Tailwind Classes |
|----------|-----------------|
| `list-style: none` | `list-none` |
| `display: flex; justify-content: center; align-items: center` | `flex justify-center items-center` |
| `margin: 0 10px` | `mx-2.5` |
| `position: relative` | `relative` |
| `position: absolute; top: -30px; left: 50%; transform: translateX(-50%)` | `absolute -top-[30px] left-1/2 -translate-x-1/2` |
| `color: #fff; padding: 6px 10px; border-radius: 5px` | `text-white px-2.5 py-1.5 rounded-md` |
| `opacity: 0; visibility: hidden` | `opacity-0 invisible` |
| `font-size: 14px` | `text-sm` |
| `transition: all 0.3s ease` | `transition-all duration-300` |
| `pointer-events: none` | `pointer-events-none` |
| `:hover opacity: 1; visibility: visible; top: -50px` | `group-hover:opacity-100 group-hover:visible group-hover:-top-[50px]` |
| Anchor `position: relative; overflow: hidden` | `relative overflow-hidden` |
| Anchor `width: 50px; height: 50px; border-radius: 50%` | `w-[50px] h-[50px] rounded-full` |
| Anchor `background-color: #fff` | `bg-white` |
| Anchor `color: #4d4d4d` | `text-[#4d4d4d]` |
| Anchor `:hover color: white; box-shadow: ...` | `hover:text-white hover:shadow-lg` |
| SVG `position: relative; z-index: 1` | `relative z-10` |
| `.filled` div `position: absolute; bottom: 0; left: 0; width: 100%` | `absolute bottom-0 left-0 w-full` |
| `.filled` div `height: 0; transition: all 0.3s ease` | `h-0 transition-all duration-300` |
| `.filled` div `:hover height: 100%` | `group-hover:h-full` |

Do NOT change the `<svg>` paths, the `data-social` attributes, or any link URLs. Only modify classes on existing elements. The component must render identically to current appearance.
</action>
<verify>
<automated>
1. `grep -c 'bulkUpdateOrden' src/Admin/firestoreApi.js | grep -v '^#' ` returns 0
2. `grep -c 'writeBatch' src/Admin/firestoreApi.js | grep -v '^#' ` returns 0
3. `grep -r '<style jsx' src/ | grep -v '^#' ` returns 0 lines (no results)
4. `grep -c 'styled' src/Componentes/Social.jsx | grep -v '^#' ` returns 0 (no styled-components import)
5. `grep -c 'StyledWrapper' src/Componentes/Social.jsx | grep -v '^#' ` returns 0
6. `grep -c 'group-hover:h-full' src/Componentes/Social.jsx | grep -v '^#' ` returns 3 (one per social icon)
7. `npm run build` completes without errors
</automated>
</verify>
<done>
bulkUpdateOrden and writeBatch removed from firestoreApi.js. Zero `<style jsx>` blocks in codebase. Social.jsx uses Tailwind classes only — no styled-components import. StyledWrapper removed. Build passes.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan removes dependencies and normalizes styles. No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm uninstall @dnd-kit/*, styled-components | accept | Removing known-unused packages; no new packages installed |
</threat_model>

<verification>
## Overall Plan Checks

After Plan 02 completes:
1. `npm run build` completes — bundle should be smaller (no @dnd-kit or styled-components)
2. `npm ls @dnd-kit/core` shows "empty" (confirm removal)
3. `grep -r '<style jsx' src/` returns zero results
4. `grep -r 'import styled from' src/` returns zero results
5. `grep -r 'bulkUpdateOrden' src/` returns zero results
6. Social icons still render on site — hover effects work via Tailwind
7. Equipo member descriptions still truncate at 3 lines (line-clamp-3 via Tailwind)
</verification>

<success_criteria>
1. Build completes without @dnd-kit/* or styled-components in bundle
2. Zero `<style jsx>` blocks in source
3. Social.jsx rendered identically with Tailwind hover effects
4. bulkUpdateOrden function removed
</success_criteria>

<output>
Create `.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md` when done
</output>
